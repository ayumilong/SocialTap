source 'https://rubygems.org'

# for development environment, use thin instead of default (webrick)
group :development do
  gem 'thin'
end

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '>=4.0.0'

# Slimmer version of Rails for RESTful APIs
gem 'rails-api'

# Use sqlite3 as the database for Active Record
gem 'sqlite3'

# Provide Elasticsearch API and transport
gem 'elasticsearch'

# To use ActiveModel has_secure_password
gem 'bcrypt', '~> 3.1.2'

# For Less compilation support - used by lib/tasks/less.rake
gem 'less-rails'
gem 'therubyracer'

# For Gnip API support
gem 'gnip-rule'

gem 'curb'

# OmniAuth and its authentication strategies
gem 'omniauth-twitter'
gem 'omniauth-identity'

# Message queueing system for interprocess communication (Analysis framework)
gem 'bunny'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# Deploy with Capistrano
# gem 'capistrano', :group => :development

# To use debugger
# gem 'ruby-debug19', :require => 'ruby-debug'
